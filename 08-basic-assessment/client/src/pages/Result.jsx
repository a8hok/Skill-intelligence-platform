import {useLocation,useNavigate} from 'react-router-dom'; export default function Result(){const {state}=useLocation();const nav=useNavigate();if(!state)return <main className="container">No result.</main>;return <main className="container"><section className="card"><span className="status">Result</span><h1>{state.topic}</h1><div className="metric">{state.score}%</div><p>{state.correct} of {state.total} correct.</p><button onClick={()=>nav('/dashboard')}>Choose another topic</button></section></main>}