import {useEffect,useState} from 'react'; import {api} from '../services/api.js'; import {useNavigate} from 'react-router-dom'; export default function Dashboard(){const [topics,setTopics]=useState([]);const nav=useNavigate();useEffect(()=>{api.topics().then(d=>setTopics(d.topics||[]))},[]);return <main className="container"><h1>Choose a topic</h1><section className="grid">{topics.map(t=><article className="card topic-card" key={t} onClick={()=>nav(`/assessment/${encodeURIComponent(t)}`)}><h3>{t}</h3><p>10 built-in questions</p></article>)}</section></main>}