# Step 9 - Gemini + Built-in Assessment
Adds a switch between deterministic built-in questions and fresh Gemini-generated questions. Put a real `GEMINI_API_KEY` in `server/.env`. If you choose Built-in, no AI key is required.
