import {useEffect,useState} from 'react'; import {api} from '../services/api.js'; import {useNavigate} from 'react-router-dom'; export default function Dashboard(){const [topics,setTopics]=useState([]);const [source,setSource]=useState('built-in');const nav=useNavigate();useEffect(()=>{api.topics().then(d=>setTopics(d.topics||[]))},[]);return <main className="container"><div className="row"><button onClick={()=>setSource('built-in')}>Built-in</button><button className="secondary" onClick={()=>setSource('gemini')}>Gemini</button></div><section className="grid" style={{marginTop:16}}>{topics.map(t=><article className="card topic-card" key={t} onClick={()=>nav(`/assessment/${encodeURIComponent(t)}?source=${source}`)}><h3>{t}</h3></article>)}</section></main>}