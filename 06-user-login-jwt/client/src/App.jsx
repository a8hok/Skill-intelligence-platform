import {Routes,Route,Navigate,Link,useNavigate} from 'react-router-dom'; import {useState} from 'react';
import Login from './pages/Login.jsx'; import Dashboard from './pages/Dashboard.jsx';
export default function App(){ const [user,setUser]=useState(()=>JSON.parse(localStorage.getItem('user')||'null')); const nav=useNavigate(); function logout(){localStorage.clear();setUser(null);nav('/login')} return <><div className="container" style={{paddingBottom:0}}>{user&&<nav className="nav"><strong>Skill Intelligence</strong><div className="navlinks"><button className="secondary" onClick={logout}>Logout</button></div></nav>}</div><Routes><Route path="/login" element={user?<Navigate to="/dashboard"/>:<Login onLogin={setUser}/>}/><Route path="/dashboard" element={user?<Dashboard user={user}/>:<Navigate to="/login"/>}/><Route path="*" element={<Navigate to={user?'/dashboard':'/login'} replace/>}/></Routes></>}