import{useLocation,useNavigate}from'react-router-dom';export default function Result(){const{state}=useLocation();const nav=useNavigate();return <main className="container"><section className="card"><h1>{state?.score}%</h1><p>Roadmap saved. Review your progress or start a targeted reassessment.</p><div className="row"><button onClick={()=>nav('/learn')}>Things to Learn</button><button className="secondary" onClick={()=>nav('/progress')}>Progress</button></div></section></main>}