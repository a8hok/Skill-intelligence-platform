import {useEffect,useState} from 'react';import {api} from '../services/api.js';import {useNavigate} from 'react-router-dom';export default function Dashboard(){const[t,setT]=useState([]);const nav=useNavigate();useEffect(()=>{api.topics().then(d=>setT(d.topics||[]))},[]);return <main className="container"><h1>Choose a topic</h1><div className="grid">{t.map(x=><article className="card topic-card" key={x} onClick={()=>nav(`/assessment/${encodeURIComponent(x)}`)}><h3>{x}</h3></article>)}</div></main>}