import {useEffect,useState} from 'react'; import {api} from '../services/api.js'; import {useNavigate} from 'react-router-dom'; export default function Dashboard({user}){const [topics,setTopics]=useState([]);const nav=useNavigate();useEffect(()=>{api.topics().then(d=>setTopics(d.topics||[]))},[]);return <main className="container"><section className="card"><span className="status">Step 7</span><h1>Welcome, {user?.name}</h1><p>Choose an engineering topic.</p></section><section className="grid" style={{marginTop:18}}>{topics.map(t=><article className="card topic-card" key={t} onClick={()=>nav(`/topic/${encodeURIComponent(t)}`)}><h3>{t}</h3><p className="muted">Assessment will be added in the next checkpoint.</p></article>)}</section></main>}