export default function App() {
  return (
    <main className="container">
      <section className="card">
        <span className="status">Step 2</span>
        <h1>Frontend + Backend</h1>
        <p>The React client and Express server run as separate applications.</p>
        <p className="muted">Backend health URL: http://localhost:4000/api/health</p>
      </section>
    </main>
  );
}
